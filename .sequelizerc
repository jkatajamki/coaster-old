const path = require('path');

const env = process.env.NODE_ENV || 'development';
const config = require('./server/config/data-source.config')[env];

module.exports = {
  config: path.resolve('server', 'config', 'data-source.config.json'),
  'models-path': path.resolve('server', 'data-access', 'models'),
  'seeders-path': path.resolve('server', 'data-access', 'seeders'),
  'migrations-path': path.resolve('server', 'data-access', 'migrations'),
};
